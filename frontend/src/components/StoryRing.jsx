import React from "react";
import {
    View,
} from "react-native";

export default function StoryRing() {
    return (
        <View
            style={{
                width: 60,
                height: 60,
                borderRadius: 30,
                borderWidth: 3,
                borderColor: "#25D366",
            }}
        />
    );
}